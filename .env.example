# Comma-separated browser origins allowed to call the API.
HDB_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Optional paths when model artifacts are stored outside the repository.
# HDB_MODEL_PATH=artifacts/price_model.cbm
# HDB_METRICS_PATH=artifacts/metrics.json
